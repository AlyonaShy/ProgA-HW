import Button from "./button";

const GreenButton = ({nameBtn}) => {
    return (
        <div className="btnContainer">
            <Button nameBtn = {nameBtn}></Button>
        </div>
    )
}
export default GreenButton;